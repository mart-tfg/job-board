import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['via.placeholder.com', 'picsum.photos', 'dummyimage.com'], // เพิ่ม dummyimage.com ที่นี่
  },};

export default nextConfig;
